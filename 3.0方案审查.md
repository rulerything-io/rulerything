# v3.0 方案审查报告：遗漏、逻辑缺陷与健壮性改进

---

## 一、功能遗漏（7 项）

### 1.1 查询日志无限增长

**问题**：`query_log` 表只有 `CREATE TABLE`，没有**保留策略**。10k 规则下假设日均 1000 次查询，年增 36 万行。性能会逐年退化。

**修复**：加自动轮换机制。
```sql
-- 额外建表：按月分表或加保留策略
CREATE TABLE query_log_archive (
    LIKE query_log
);
-- 或者加 delete 策略（SQLite 原生不支持 TTL，需应用层处理）
```
或用**按周分表** `query_log_YYYY_WW`，自动清理 8 周前的数据。

### 1.2 内存索引与 SQLite 不同步

**问题**：查询路径走内存 `EverythingStyleIndex`，但 AI 自动入库/AutoEvolver 修改走 SQLite。两者之间没有**同步校验**。如果任一方写入失败，两者会静默不一致。

**修复**：每次写入操作**双写**（SQLite + 内存索引），任一失败则回滚两者。加定时对账：
```python
def reconcile():
    sqlite_count = db.query("SELECT COUNT(*) FROM rules")
    mem_count = len(index._rules)
    if sqlite_count != mem_count:
        logger.error("INDEX_DESYNC", f"{sqlite_count} vs {mem_count}")
        index.rebuild_from_sqlite()  # 以 SQLite 为准重建
```

### 1.3 PMI 依赖挖掘冷启动空结果

**问题**：共现分析需要**查询历史**。新系统/数据迁移后 query_log 为空，`dep_miner` 连续一周产出空结果，但没有任何告警。

**修复**：冷启动期（query_log < 100 条）改用**内容相似度**作为后备。仪表板显示"依赖图构建中(%)"进度。

### 1.4 无 AI 调用预算控制

**问题**：AI Bridge 没有**每日预算/速率限制**。1000 次低置信度查询 = 1000 次 API 调用，按 Claude 价格约 $30-100。没有控制的话，一次异常流量就能烧光预算。

**修复**：
```python
# 每日 AI 预算控制
ai_budget = {
    "daily_limit_usd": 5.0,
    "daily_cost": 0.0,
    "last_reset": "2026-06-10",
    "per_query_max_cost": 0.05,  # 单次调用预算
}
# 超预算后降级为 v2.0 模式（不调 AI）
```

### 1.5 冷规则死循环

**问题**："365 天未命中 → 归档"没有问题。但**归档后永远不会再被命中**（已从活跃索引移除），因此永远不会有"重新激活"的机会。如果某条规则只是季节性有用（比如"圣诞促销相关SQL优化"），归档后即便到季节也不会重新出现。

**修复**：归档规则不物理删除，而是移入**冷存储**（单独的 SQLite 表）。当缺口检测发现某高频查询与已归档规则匹配时，自动**解冻**该规则。
```sql
CREATE TABLE rules_cold (
    LIKE rules,
    archived_at TEXT NOT NULL,
    original_category TEXT
);
```

### 1.6 管理面板未更新

**问题**：`/static/dashboard.html` 仍为 v2.0 的 UI，没有 v3.0 模块的状态入口。无法直观查看提案列表、依赖图、覆盖率。等于 v3.0 的 API 没有前端。

**修复**：最低成本方案——加一个纯 JSON 的 `/v3/status` 汇总接口，dashboard 加一个 iframe 或 tab 展示。

### 1.7 无告警出口

**问题**：方案说"仅异常告警"，但没有定义**告警怎么出去**。没有邮件、没有 webhook、没有日志聚合。自动回滚失败发生在凌晨 3 点，没人知道。

**修复**：加一个简单的**通知接口**：
```python
class AlertChannel(ABC):
    @abstractmethod
    def send(self, title: str, body: str, severity: str): ...

class LogAlert(AlertChannel):      # 写日志（默认必有）
    def send(self, title, body, severity):
        logger.error(f"[ALERT][{severity}] {title}: {body}")

class WebhookAlert(AlertChannel):  # 可选配置
    def send(self, title, body, severity):
        requests.post(self.webhook_url, json={"text": f"[{severity}] {title}\n{body}"})
```

默认只写日志，可配置 webhook 对接企业微信/钉钉/Slack。

---

## 二、逻辑缺陷（6 项）

### 2.1 AI 验证的逻辑矛盾 🔴

**问题**：方案说"低置信度（< 0.7）→ 调 AI" 然后 "用系统规则验证 AI 答案（评分 < 0.3 打回）"。

**但系统正是因为不知道答案才去调 AI 的**，拿什么验证 AI 的回答？系统只能做**一致性检查**（AI 回答是否与已有规则矛盾），无法做**正确性检查**。

```
用户问："Rust 的 lifetime 怎么用？"
→ 系统没有 Rust 规则（置信度 0）→ 调 AI
→ AI 给了详细解释
→ 系统用"已有规则"验证 → 但没有任何 Rust 规则 → 验证评分 = 0
→ AI 回答被打回 → 用户拿到空结果
→ 死循环：每次都调 AI，每次都验证失败
```

**修复**：
- 验证评分 < 0.3 不意味着"打回"，而是"系统无相关知识，无法验证"
- 评分含义应该区分：
  - `0.7-1.0`：与系统知识一致（可信）
  - `0.3-0.7`：部分一致（部分可信）
  - `0-0.3`：**无法验证**或与系统知识矛盾
  - `< 0`（负数）：与系统知识矛盾（不可信）

当评分为"无法验证"时，**直接返回 AI 回答**并标注"未经系统验证"。

### 2.2 共现 ≠ 依赖 🔴

**问题**：`dep_miner` 用 PMI（共现频率）挖掘依赖关系。但**共现不等于依赖**。两条规则经常一起出现可能是因为它们都属于同一个宽泛分类（比如两条"Python 性能优化"规则），它们之间并没有依赖关系。

```
query: "Python 列表推导式性能"
  → 返回 python/010 (列表推导式) 和 python/011 (生成器)
  → PMI 认为两者有依赖关系
  → 实际上它们是并列关系，不是依赖关系
```

**修复**：将"依赖"弱化为"关联"，在数据模型和 UI 上区分：
- `关联 (related_to)`：共现挖掘的弱关系，表示"经常一起被查"
- `依赖 (depends_on)`：仅在内容相似度 + 时序转移概率均高时才标记
- UI 上关联用虚线、依赖用实线

### 2.3 性能验证的统计谬误

**问题**：演化执行后 `validate_result()` 检测性能是否退化。但用**单次采样**判断趋势会误判。

```
演化后第 1 秒：延迟 150ms（基线 100ms）→ 判定退化 → 回滚
但实际上只是 GC 暂停，下一秒就恢复 90ms
不必要的回滚浪费了资源
```

**修复**：使用**滑动窗口统计检验**，不是单点比较：
```python
def is_degraded(baseline: list[float], current: list[float]) -> bool:
    """
    使用 Welch's t-test 或简单阈值比较。
    要求至少采集 N 个样本点，且均值差 > 2σ 才判定退化。
    """
    if len(current) < 10:  # 样本不足，暂不判定
        return False
    mu_b, mu_c = np.mean(baseline), np.mean(current)
    sigma_b = max(np.std(baseline), 0.01)
    effect_size = (mu_c - mu_b) / sigma_b
    return effect_size > 0.5  # Cohen's d > 0.5 才算显著退化
```

### 2.4 缺口检测的阈值风暴

**问题**：`gap_detector` 用固定阈值 `相似度 < 0.3 且频率 > 5 次/周`。10k 规则量下，如果系统覆盖不够好，可能每周产生 **50-100 个缺口** → AI 批量生成 100 条规则 → 质量不可控。

**修复**：改为**动态阈值** + **数量上限**：
```python
# 每周最多提炼 N 条规则
MAX_NEW_RULES_PER_WEEK = 15

# 缺口排序后只取前 N 个
gaps = detect_gaps()
gaps.sort(key=lambda g: g.frequency * g.severity, reverse=True)
top_gaps = gaps[:MAX_NEW_RULES_PER_WEEK]

# 阈值自适应：如果一周内通过率 < 50%，下周自动收紧
if last_week_approval_rate < 0.5:
    threshold *= 1.1  # 更严格
```

### 2.5 相同提案重复触发

**问题**：`auto_proposer` 没有**去重**。30 秒循环检测到延迟高 → 生成"重建索引"提案 → 执行前下一次循环又检测到延迟高 → 再生成一个。两个相同提案同时在执行。

**修复**：提案去重键 `{proposal_type}:{target}`：
```python
class AutoProposer:
    def __init__(self):
        self.active_proposals: set[str] = set()  # 正在执行中的提案去重键
        self.cooldown: dict[str, datetime] = {}   # 每种提案的冷却期

    def _dedup_key(self, proposal) -> str:
        return f"{proposal.type}:{proposal.target}"

    def generate(self, metrics):
        for candidate in self._detect(metrics):
            key = self._dedup_key(candidate)
            if key in self.active_proposals:
                continue  # 已在执行
            if key in self.cooldown and cooldown[key] > now:
                continue  # 冷却中
            self.active_proposals.add(key)
            self._execute(candidate)
            # 执行完后设置冷却期（同一提案 N 小时内不重复）
            self.cooldown[key] = now + timedelta(hours=4)
```

### 2.6 快照数量失控

**问题**：方案设定 `max_snapshots=50`。但按计划每天至少 3 个快照（quality_scan + dep_refresh + cold_archive），50 个 = 17 天用完。17 天后要么拒绝新快照（演化无法执行），要么静默覆盖。

**修复**：快照分级淘汰：
```python
# 保留策略
SNAPSHOT_RETENTION = {
    "last_24h": None,       # 全部保留
    "last_7d":  "1 per 6h", # 每 6 小时留一个
    "last_30d": "1 per day", # 每天留一个
    "older":     None,       # 全删
}
# 或者更简单：保留最近 50 个，新快照写入时删除最旧的
if len(snapshots) >= MAX_SNAPSHOTS:
    oldest = snapshots[0]
    delete_snapshot(oldest)
```

---

## 三、健壮性改进（10 项）

### 3.1 熔断器（Circuit Breaker）

**问题**：连续失败的提案会反复重试，浪费资源、加重问题。

**方案**：每个提案类型带熔断器。

```python
class CircuitBreaker:
    """每类提案独立熔断。"""
    def __init__(self, failure_threshold=3, recovery_timeout=300):
        self.failures: dict[str, int] = defaultdict(int)
        self.last_failure: dict[str, datetime] = {}
        self.failure_threshold = failure_threshold
        self.recovery_timeout = recovery_timeout  # 秒

    def is_open(self, proposal_type: str) -> bool:
        """熔断是否已打开（= 拒绝执行）。"""
        if self.failures[proposal_type] >= self.failure_threshold:
            elapsed = (datetime.now() - self.last_failure[proposal_type]).total_seconds()
            if elapsed > self.recovery_timeout:
                self.failures[proposal_type] = 0  # 半开
                return False
            return True
        return False

    def record_failure(self, proposal_type: str):
        self.failures[proposal_type] += 1
        self.last_failure[proposal_type] = datetime.now()

    def record_success(self, proposal_type: str):
        self.failures[proposal_type] = 0
```

### 3.2 模块级故障隔离

**问题**：30 秒主循环在一个大 try 里。`dep_miner` 抛异常 = 整个循环挂掉。

**方案**：每个模块独立 try/except，单模块故障不影响其他模块。

```python
def main_loop():
    results = {}
    for module_name, module_fn in [
        ("dep_miner", refresh_deps),
        ("proposer", run_proposals),
        ("gap_detector", scan_gaps),
        ("evolver", check_evolution),
        ("ai_ingest", process_pending_ai),
    ]:
        try:
            results[module_name] = module_fn()
        except Exception as e:
            logger.error(f"MODULE_FAILURE:{module_name}", str(e))
            results[module_name] = {"status": "failed", "error": str(e)}
            # 不要 raise，继续下一个模块
    return results
```

### 3.3 优雅降级（Degraded Mode）

**问题**：SQLite 故障 → 全系统挂。AI Bridge 故障 → 查询路径挂。

**方案**：每个外部依赖都有降级路径。

```python
class SystemMode(Enum):
    NORMAL = "normal"           # 一切正常
    STORAGE_DEGRADED = "storage_degraded"  # SQLite 故障，只读
    AI_UNAVAILABLE = "ai_down"  # AI API 不可用
    INDEX_ONLY = "index_only"   # 最小模式：只响应查询

# 降级规则：
# SQLite 不可用 → 切换到 INDEX_ONLY（内存索引仍可查询）
# AI 不可用 → 降级到 v2.0 查询（不调 AI）
# dep_miner 异常 → 降级到上次成功的依赖图（不重新挖掘）
# 索引异常 → 尝试重建，失败则重启
```

### 3.4 操作超时保护

**问题**：索引重建可能耗时 > 5 分钟。`dep_miner` 全量挖掘可能 > 10 分钟。没有超时切断，会堵塞主循环。

**方案**：所有后台操作带超时 + 守护线程。

```python
def run_with_timeout(fn, timeout_seconds, default_return=None):
    """在独立线程运行，超时则放弃。"""
    result = [default_return]
    def target():
        try:
            result[0] = fn()
        except Exception as e:
            result[0] = default_return
            logger.error("TIMEOUT_TARGET", str(e))
    t = threading.Thread(target=target, daemon=True)
    t.start()
    t.join(timeout_seconds)
    if t.is_alive():
        logger.warning("OP_TIMEOUT", f"{fn.__name__} exceeded {timeout_seconds}s")
        return default_return
    return result[0]
```

每个模块配置独立超时：
```yaml
v3:
  timeouts:
    dep_miner: 120        # 秒
    gap_detector: 60
    auto_evolver: 300     # 含快照和回滚
    index_rebuild: 600
    ai_call: 30           # AI API 调用超时
  degraded_after_timeout: true  # 超时后降级该模块
```

### 3.5 启动自检（Startup Health Check）

**问题**：SQLite 表缺失、索引未构建、配置错误 → 启动成功但运行时报错。

**方案**：
```python
def startup_self_check(config):
    checks = []

    # 1. SQLite 表完整性
    required_tables = ["rules", "query_log", "metrics_log", "proposals", "snapshots"]
    existing = db.execute("SELECT name FROM sqlite_master WHERE type='table'").fetchall()
    existing = {r[0] for r in existing}
    for t in required_tables:
        checks.append(("table_exists", t, t in existing))

    # 2. 内存索引一致性
    mem_count = len(index._rules)
    db_count = db.execute("SELECT COUNT(*) FROM rules").fetchone()[0]
    checks.append(("index_sync", "", mem_count == db_count))

    # 3. 配置有效性
    checks.append(("ai_budget", "", config.get("ai_bridge", {}).get("daily_limit_usd", 0) > 0))
    checks.append(("snapshot_dir", "", os.path.exists(config["snapshot_dir"])))

    # 4. 端口可用性
    # ...

    failed = [c for c in checks if not c[2]]
    if failed:
        logger.critical("STARTUP_CHECKS_FAILED", failed)
        # 非致命失败降级，致命失败退出
        for check in failed:
            if check[0] in FATAL_CHECKS:
                sys.exit(1)
```

### 3.6 操作审计日志

**问题**：自动操作全是黑盒。出问题后不知道"发生了什么"。

**方案**：所有自动化操作写不可变审计日志。
```python
@dataclass
class AuditEntry:
    timestamp: str
    action: str           # proposal_execute | evolver_run | ai_ingest | rollback
    module: str
    actor: str            # "auto_proposer" | "auto_evolver" | "ai_bridge"
    target: str           # 操作对象
    before_state: str     # 快照 ID 或状态摘要
    after_state: str
    result: str           # success | failed | rolled_back
    duration_ms: int
    error: str = ""

# 审计日志独立存储，不怕回滚影响
# 表结构：
CREATE TABLE audit_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp TEXT NOT NULL,
    action TEXT NOT NULL,
    module TEXT NOT NULL,
    actor TEXT NOT NULL,
    target TEXT,
    before_snapshot TEXT,
    after_snapshot TEXT,
    result TEXT NOT NULL,
    duration_ms INTEGER,
    error TEXT
);
```

审计日志不参与系统回滚——即使系统回滚到 1 小时前，审计日志仍保留全部记录。

### 3.7 运行时配置热加载

**问题**：修改阈值/开关需要重启服务。重启期间不执行任何自动操作。

**方案**：从 SQLite 加载配置，支持热更新。
```python
# config_runtime 表
CREATE TABLE config_runtime (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL,
    updated_at TEXT NOT NULL
);

class RuntimeConfig:
    """运行时配置，热加载。"""
    def get(self, key: str, default=None):
        # 先从内存缓存读，每 60 秒同步一次 SQLite
        # 修改配置只需 UPDATE 这张表，系统自动生效

# API 方便修改
POST /v3/config  {"key": "gap_detector.max_rules_per_week", "value": "20"}
```

### 3.8 主循环心跳

**问题**：主循环停掉了（线程死锁、未捕获异常）不会有任何告警。

**方案**：
```python
def main_loop():
    last_heartbeat = datetime.now()

    while running:
        # ... 执行各模块 ...

        # 更新心跳
        last_heartbeat = datetime.now()

# 独立监控线程
def heartbeat_monitor():
    while True:
        time.sleep(60)
        if (datetime.now() - last_heartbeat).seconds > 120:
            # 主循环超过 2 分钟没有心跳
            alert("MAIN_LOOP_STALLED", "主循环疑似卡死")
            # 尝试重启...
```

**健康 API**：
```
GET  /v3/health → {
    "status": "ok",
    "uptime": 12345,
    "last_heartbeat": "2026-06-10T12:00:00",
    "module_status": {
        "dep_miner": "ok",
        "auto_proposer": "ok",
        "gap_detector": "degraded",  # 上次运行超时
        "auto_evolver": "ok",
        "ai_bridge": "disabled"
    },
    "last_audit": {
        "action": "dep_refresh",
        "result": "success",
        "timestamp": "2026-06-10T11:55:00"
    }
}
```

### 3.9 自动回滚的级联处理

**问题**：如果 Proposal A 执行后修改了规则 X，Proposal B 又修改了规则 X。此时回滚 A 会覆盖 B 的改动。

**方案**：版本检查 + 冲突检测。
```python
def rollback(proposal_id):
    proposal = get_proposal(proposal_id)
    snapshot = load_snapshot(proposal.snapshot_id)

    conflicts = []
    for rule_id in snapshot.modified_rules:
        current_version = db.execute(
            "SELECT version FROM rules WHERE id = ?", (rule_id,)
        ).fetchone()[0]
        snapshot_version = snapshot.get_rule_version(rule_id)
        if current_version > snapshot_version + 1:
            # 回滚会丢失中间版本的改动
            conflicts.append(rule_id)

    if conflicts:
        # 自动尝试三方合并（简单版本：保留较新版本）
        # 或标记冲突等待处理
        mark_conflict(proposal_id, conflicts)

    # 执行回滚...
```

### 3.10 数据完整性校验

**问题**：长时间运行后，SQLite 数据和内存索引可能因 bug 或崩溃而不一致。

**方案**：每日完整性校验：
```python
def daily_integrity_check():
    checks = []

    # 1. 引用完整性：所有 duplicate_of 指向存在的规则
    orphans = db.execute("""
        SELECT id, duplicate_of FROM rules
        WHERE duplicate_of IS NOT NULL
        AND duplicate_of NOT IN (SELECT id FROM rules)
    """).fetchall()
    checks.append(("orphan_duplicates", len(orphans) == 0))

    # 2. 置信度范围
    out_of_range = db.execute("""
        SELECT COUNT(*) FROM rules
        WHERE confidence < 0 OR confidence > 1
    """).fetchone()[0]
    checks.append(("confidence_range", out_of_range == 0))

    # 3. 快照文件存在性
    missing_snapshots = []
    for row in db.execute("SELECT id, storage_path FROM snapshots").fetchall():
        if not os.path.exists(row[1]):
            missing_snapshots.append(row[0])
    checks.append(("snapshot_files", len(missing_snapshots) == 0))

    # 4. 审计日志的 snapshot 引用
    # ...

    # 记录结果，但除非严重问题不阻断运行
    for check, passed in checks:
        if not passed:
            logger.warning("INTEGRITY_CHECK_FAILED", check)
```

---

## 四、总结：方案修正清单

以上共识别 **23 个问题**，按严重程度：

| 严重度 | 数量 | 必须修复 |
|--------|------|---------|
| 🔴 逻辑缺陷 | 6 | **是** — 不改会导致错误行为 |
| 🟡 功能遗漏 | 7 | **建议** — 不改会导致运维问题 |
| 🔵 健壮性 | 10 | **推荐** — 不改在压力下会出问题 |

建议在进入实施前，将上述修复整合到方案文档中。特别是 **3 个 🔴 逻辑缺陷**（AI验证矛盾、共现≠依赖、性能验证误判）必须在设计阶段解决。
