{"id": "rust/001", "title": "Rust Async Programming", "content": "# Rust Async Programming\n\n## Async/Await Fundamentals\n\n- Use `async fn` for functions that perform asynchronous operations\n- Always `.await` async function calls within async contexts\n- Prefer `tokio` runtime for most async applications\n- Use `async move` closures when moving ownership into async blocks\n\n## Task Management\n\n- Spawn tasks with `tokio::spawn()` for concurrent execution\n- Use `tokio::select!` for waiting on multiple async operations\n- Implement proper cancellation with `CancellationToken`\n- Avoid blocking operations in async functions\n\n## Error Handling\n\n- Use `Result` types consistently in async functions\n- Prefer `?` operator for error propagation in async contexts\n- Handle timeout scenarios with `tokio::time::timeout`\n- Use `anyhow` for application-level async error handling\n\n## Common Patterns\n\n### Async Streams\n- Use `tokio_stream` for working with async iterators\n- Implement `Stream` trait for custom async data sources\n- Use `StreamExt` methods for stream transformations\n\n### Channels\n- Use `tokio::sync::mpsc` for multi-producer, single-consumer channels\n- Use `tokio::sync::broadcast` for broadcasting to multiple receivers\n- Use `tokio::sync::oneshot` for single-value communication\n\n## Performance Guidelines\n\n- Minimize `.await` points to reduce context switching overhead\n- Use `tokio::task::yield_now()` to yield control in CPU-intensive loops\n- Profile async code with `tokio-console` for runtime insights\n- Batch operations when possible to reduce async overhead\n\n## Best Practices\n\n- Keep async functions focused and composable\n- Use `#[tokio::test]` for testing async functions\n- Avoid mixing blocking and async code\n- Use `spawn_blocking` for CPU-intensive or blocking operations\n- Structure async code to avoid excessive nesting\n\n## Common Anti-Patterns\n\n- **Blocking in Async**: Never use `std::thread::sleep` in async functions\n- **Excessive Spawning**: Don't spawn tasks for every small operation\n- **Missing Error Handling**: Always handle errors in spawned tasks\n- **Resource Leaks**: Ensure proper cleanup of async resources", "category": "rust", "tags": ["rust"], "confidence": 0.85, "verifier": "manual", "version": 1, "evolution_log": ["v1: 从 awesome-rules/rust/rust-async-programming.md 导入"], "created_at": "2026-06-06T14:19:41.323692", "last_verified": "2026-06-06T14:19:41.323692", "hit_count": 0}
{"id": "rust/002", "title": "Rust Best Practices", "content": "# Rust Best Practices\n\n## Key Principles\n\n- **Memory Safety**: Leverage Rust's ownership system to prevent memory-related bugs\n- **Zero-Cost Abstractions**: Use high-level constructs without runtime overhead\n- **Fearless Concurrency**: Write safe concurrent code using Rust's type system\n- **Error Handling**: Use `Result` and `Option` types for robust error handling\n- **Performance**: Write efficient code with predictable performance characteristics\n\n## Coding Standards\n\n- Use `cargo fmt` for consistent code formatting\n- Run `cargo clippy` to catch common mistakes and improve code quality\n- Write comprehensive tests with `cargo test`\n- Use descriptive variable and function names\n- Prefer immutable bindings by default (`let` over `let mut`)\n- Use `snake_case` for variables and functions, `PascalCase` for types\n- Keep functions small and focused on a single responsibility\n\n## Ownership and Borrowing\n\n- Understand the three rules of ownership: each value has one owner, only one owner at a time, value is dropped when owner goes out of scope\n- Use borrowing (`&`) instead of moving when you don't need ownership\n- Prefer borrowing over cloning for performance\n- Use `Rc<RefCell<T>>` or `Arc<Mutex<T>>` sparingly for shared ownership\n- Leverage lifetime annotations to express relationships between references\n\n## Error Handling\n\n- Use `Result<T, E>` for recoverable errors\n- Use `Option<T>` for nullable values instead of null pointers\n- Prefer `?` operator for error propagation\n- Create custom error types using `thiserror` crate\n- Use `anyhow` for application-level error handling\n- Avoid `unwrap()` and `expect()` in production code\n\n## Dependencies\n\n- **Core Libraries**: `serde`, `tokio`, `clap`, `thiserror`, `anyhow`\n- **Development**: `cargo-watch`, `cargo-audit`, `cargo-tarpaulin`\n- **Testing**: Built-in test framework, `proptest` for property testing\n- **Linting**: `clippy`, `rustfmt`\n\n## Performance Guidelines\n\n- Use `Vec<T>` for dynamic arrays, prefer `&[T]` for function parameters\n- Use `String` for owned strings, `&str` for string slices\n- Prefer iterators over explicit loops for better performance and readability\n- Use `collect()` judiciously - consider streaming operations\n- Profile with `cargo bench` and `perf` tools\n- Avoid unnecessary allocations and clones\n\n## Conventions\n\n1. **Project Structure**: Use `src/lib.rs` for libraries, `src/main.rs` for binaries\n2. **Module Organization**: One module per file, use `mod.rs` for module directories\n3. **Documentation**: Write doc comments with `///` for public APIs\n4. **Testing**: Place unit tests in the same file using `#[cfg(test)]`\n5. **Integration Tests**: Place in `tests/` directory\n6. **Examples**: Provide runnable examples in `examples/` directory\n7. **Cargo.toml**: Keep dependencies minimal and well-documented\n8. **Version Pinning**: Use semantic versioning and appropriate version constraints", "category": "rust", "tags": ["rust"], "confidence": 0.85, "verifier": "manual", "version": 1, "evolution_log": ["v1: 从 awesome-rules/rust/rust-best-practices.md 导入"], "created_at": "2026-06-06T14:19:41.323692", "last_verified": "2026-06-06T14:19:41.323692", "hit_count": 0}
{"id": "rust/003", "title": "Rust Ownership and Borrowing Guidelines", "content": "# Rust Ownership and Borrowing Guidelines\n\n## Ownership Principles\n\n- **Single Owner**: Each value has exactly one owner at any given time\n- **Move Semantics**: Values are moved by default, not copied\n- **RAII**: Resources are automatically cleaned up when owners go out of scope\n- **No Garbage Collector**: Memory management happens at compile time\n\n## Borrowing Best Practices\n\n### Immutable Borrowing\n- Use `&T` for read-only access to data\n- Multiple immutable borrows are allowed simultaneously\n- Prefer immutable borrows when you don't need to modify data\n- Pass `&str` instead of `String` for function parameters when possible\n\n### Mutable Borrowing\n- Use `&mut T` for exclusive write access\n- Only one mutable borrow allowed at a time\n- No other borrows (mutable or immutable) can coexist with a mutable borrow\n- Keep mutable borrow scopes as small as possible\n\n## Common Patterns\n\n### Function Parameters\n```rust\n// Prefer borrowing over ownership\nfn process_data(data: &[u8]) -> Result<(), Error> { /* ... */ }\n\n// Use owned types when you need to store or transform\nfn store_data(data: String) -> Result<(), Error> { /* ... */ }\n```\n\n### Return Values\n- Return owned types when creating new data\n- Return borrowed types when referencing existing data\n- Use lifetime parameters when returning references\n\n### Collections\n- Use `Vec<T>` for owned collections\n- Use `&[T]` for borrowed slices\n- Use `HashMap<K, V>` for owned key-value pairs\n\n## Lifetime Guidelines\n\n### Explicit Lifetimes\n- Use lifetime parameters when compiler cannot infer relationships\n- Keep lifetime annotations minimal and clear\n- Use descriptive lifetime names (`'a`, `'input`, `'output`)\n\n### Lifetime Elision\n- Rely on lifetime elision rules when possible\n- Understand when explicit lifetimes are required\n- Single input lifetime is assigned to all outputs\n\n## Anti-Patterns to Avoid\n\n- **Excessive Cloning**: Don't clone unnecessarily to avoid borrow checker\n- **Reference Cycles**: Avoid `Rc<RefCell<T>>` cycles that cause memory leaks\n- **Fighting the Borrow Checker**: Restructure code instead of using unsafe\n- **Premature Arc/Mutex**: Use shared ownership only when necessary\n\n## Advanced Patterns\n\n### Interior Mutability\n- Use `Cell<T>` for `Copy` types that need interior mutability\n- Use `RefCell<T>` for runtime borrow checking\n- Use `Mutex<T>` for thread-safe interior mutability\n\n### Smart Pointers\n- `Box<T>` for heap allocation and owned data\n- `Rc<T>` for reference counting in single-threaded contexts\n- `Arc<T>` for atomic reference counting in multi-threaded contexts\n\n## Common Solutions\n\n### Multiple Mutable References\n- Split borrows using index access or separate methods\n- Restructure data to avoid borrowing conflicts\n- Use interior mutability patterns when appropriate\n\n### Lifetime Issues\n- Avoid returning references to local variables\n- Use owned types when lifetime relationships are complex\n- Consider using lifetime bounds on generic types\n\n## Testing Ownership Patterns\n\n- Write tests that verify memory safety properties\n- Test edge cases with borrowing and lifetimes  \n- Use `cargo miri` to catch undefined behavior\n- Profile memory usage patterns in complex applications", "category": "rust", "tags": ["rust"], "confidence": 0.85, "verifier": "manual", "version": 1, "evolution_log": ["v1: 从 awesome-rules/rust/rust-ownership-borrowing.md 导入"], "created_at": "2026-06-06T14:19:41.323692", "last_verified": "2026-06-06T14:19:41.323692", "hit_count": 0}
{"id": "rust/004", "title": "Rust Async Programming & Concurrency Patterns", "content": "You are an expert in Rust async programming and concurrency.\n\n## Async Runtime\n- Use tokio for production async runtime (multi-threaded, work-stealing)\n- Use async-std for simpler single-threaded async needs\n- Use smol for minimal async runtime\n- Use tokio::main for application entry points\n- Design async functions to perform I/O, not CPU-bound work\n\n## Fundamental Patterns\n- Use tokio::spawn for fire-and-forget concurrent tasks\n- Use tokio::join! for awaiting multiple futures simultaneously\n- Use tokio::select! for racing multiple futures (first complete wins)\n- Use tokio::try_join! for fallible concurrent operations\n- Use JoinSet for managing dynamic groups of tasks\n- Use tokio::sync::mpsc for multi-producer channel communication\n- Use tokio::sync::oneshot for one-shot responses\n- Use tokio::sync::watch for state change broadcasting\n\n## Shared State\n- Never hold a Mutex guard across .await points (deadlock)\n- Use tokio::sync::Mutex (not std::sync::Mutex) for held-across-await\n- For high contention: use actors (channels) over shared state\n- Use Arc<Mutex<T>> for shared state across tasks\n- Use Arc<RwLock<T>> for read-heavy shared state\n- Use atomics (AtomicUsize, AtomicBool) for simple shared counters/flags\n\n## Performance\n- Profile: tokio-console for async task diagnostics\n- Buffer/limit channels with bounded sizes (backpressure)\n- Use semaphores along critical resources to limit concurrency\n- Batch small operations: tokio::time::interval for rate limiting\n- Use tokio::task::spawn_blocking for CPU-bound or blocking operations\n- Use mio for custom event loops when tokio abstraction is too heavy\n\n## Error Handling in Async\n- Use anyhow for application-level error handling (? operator)\n- Use thiserror for library error types\n- Use .map_err() to convert errors in async chains\n- Handle task panics with JoinHandle::is_finished and catching\n- Use CancellationToken for graceful shutdown\n\n## Testing Async\n- Use tokio::test for async test functions\n- Use tokio::time::pause for deterministic time-based tests\n- Use Mock or test doubles for external services\n- Use assert_matches macro for pattern matching assertions", "category": "rust", "tags": ["rust", "async", "concurrency", "tokio"], "confidence": 0.88, "verifier": "manual", "version": 1, "parent_id": null, "duplicate_of": null, "evolution_log": [], "created_at": "2026-06-07T13:02:38.467254", "last_verified": null, "expires_at": null, "hit_count": 0, "last_hit": null, "content_hash": ""}
{"id": "rust/005", "title": "Rust Error Handling & Design Patterns", "content": "You are an expert in Rust error handling strategies and design patterns.\n\n## Error Handling Strategy\n- Use Result<T, E> for recoverable errors, panic! for unrecoverable\n- Use thiserror for library error types (derive macros, Display/Error auto)\n- Use anyhow::Error for application error handling (? propagation)\n- Use anyhow::Context for adding context to errors (.context()/.with_context())\n- Use custom error enums with thiserror for typed errors\n- Implement From traits for automatic error conversion (? operator)\n- Use .unwrap_or(), .unwrap_or_else(), .ok(), .map_err() for Result conversion\n\n## Pattern: Builder\n- Use builder pattern for complex object construction\n- Return Result from build() if validation needed\n- Use typestate pattern to enforce required fields at compile time\n- Use Into trait for flexible builder method parameters\n\n## Pattern: Newtype\n- Wrap primitive types for type safety: struct Email(String)\n- Implement Deref for transparent access when appropriate\n- Implement From/Into for conversions\n- Use derive(newtype) or wrapper macros for boilerplate\n\n## Pattern: RAII (Resource Acquisition Is Initialization)\n- Use Drop trait for automatic resource cleanup\n- Guard pattern: acquire on creation, release on Drop\n- Use for mutex locks, file handles, database connections\n- Return &T or &mut T from guards, never the guard itself\n\n## Pattern: Type State\n- Encode state transitions in the type system (compile-time checking)\n- Generic structs with phantom type parameters for state\n- Methods only available in specific states\n- Example: Connection<Closed>, Connection<Open>, Connection<Authenticated>\n\n## Pattern: Strategy / Dependency Injection\n- Use trait objects (Box<dyn Trait>) for runtime polymorphism\n- Use generics (impl Trait / T: Trait) for compile-time polymorphism\n- Use closures for simple strategy injection\n- Use Tower service trait for middleware abstraction\n\n## Unsafe Code Guidelines\n- Minimize unsafe blocks; audit thoroughly\n- Document safety preconditions in SAFETY comments\n- Encapsulate unsafe in safe abstractions\n- Use Miri to detect undefined behavior\n- Use raw pointer hygiene: non-null, aligned, valid, unique\n- Prefer std::mem::ManuallyDrop for manual drop control", "category": "rust", "tags": ["rust", "error-handling", "design-patterns", "unsafe"], "confidence": 0.88, "verifier": "manual", "version": 1, "parent_id": null, "duplicate_of": null, "evolution_log": [], "created_at": "2026-06-07T13:02:38.467254", "last_verified": null, "expires_at": null, "hit_count": 0, "last_hit": null, "content_hash": ""}
{"id": "rust/006", "title": "Rust Systems Programming & FFI", "content": "You are an expert in Rust systems programming and FFI.\n\n## Systems Programming Patterns\n- Use no_std for embedded/bare-metal targets (no standard library)\n- Use alloc crate for heap allocation in no_std environments\n- Use core::ptr for raw pointer manipulation (with safety wrappers)\n- Use core::mem for manual memory operations\n- Use core::sync::atomic for lock-free primitives\n- Use repr(C) for structs with specific memory layout\n- Use repr(align(n)) and repr(packed) for alignment control\n\n## Memory Management\n- Use Box for heap allocation, Rc/Arc for shared ownership\n- Use Pin for self-referential types (async state machines)\n- Use MaybeUninit for lazy initialization (avoid zero-cost init)\n- Use NonNull for guaranteed non-null pointer types\n- Use Cell/RefCell for interior mutability (single-thread)\n- Use Mutex/RwLock for interior mutability (multi-thread)\n- Use Cow (Clone-on-Write) for borrowed-or-owned data\n\n## FFI (Foreign Function Interface)\n- Use repr(C) on structs passed across FFI boundary\n- Use extern \"C\" for function declarations\n- Use unsafe blocks for all FFI calls\n- Use std::ffi::CStr/CString for C string conversion\n- Use std::os::raw for C type aliases (c_int, c_char, c_void)\n- Check null pointers before dereferencing FFI returns\n- Use Abi: #[repr(C, align(8))] for C-compatible layout\n- Use bindgen for auto-generating FFI bindings from C headers\n- Use cbindgen for generating C headers from Rust code\n- Never pass Rust closures or trait objects through FFI\n\n## Build & Tooling\n- Use cross-compilation: cargo build --target <triple>\n- Use cargo-expand for macro expansion debugging\n- Use cargo-asm for assembly output inspection\n- Use cargo-bloat for binary size analysis\n- Use cargo-criterion for benchmarking\n- Use linker scripts and section placement for embedded\n\n## Performance\n- Use #[inline] for small, hot-path functions\n- Use #[repr(transparent)] for zero-cost wrappers\n- Use const generics for compile-time sized arrays\n- Use SIMD with std::simd or core::simd (nightly)\n- Use align_to for SIMD-friendly memory access\n- Profile with perf (Linux), Instruments (macOS), VTune\n- Use optimized allocators: mimalloc, jemalloc over system allocator", "category": "rust", "tags": ["rust", "systems", "ffi", "no-std", "embedded"], "confidence": 0.85, "verifier": "manual", "version": 1, "parent_id": null, "duplicate_of": null, "evolution_log": [], "created_at": "2026-06-07T13:02:38.467254", "last_verified": null, "expires_at": null, "hit_count": 0, "last_hit": null, "content_hash": ""}
