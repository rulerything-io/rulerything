# 规则系统 v3.0 最终方案

> 目标：10k 规则规模，全自动运行，遵循 Everything 设计哲学。

---

## 一、Everything 哲学合规性

### Everything 五条铁律（来自 `enhanced_index.py`）

| 铁律 | v3.0 合规情况 | 说明 |
|------|-------------|------|
| ① 全内存索引 | ✅ | 查询路径仍走 `EnhancedEverythingIndex`（内存） |
| ② 排序数组 + 二分查找 | ✅ | 不变 |
| ③ 零外部依赖 | ⚠️ **核心路径零外部依赖，侧模块可选** | 见下方详细说明 |
| ④ 实时增量更新 | ✅ | SQLite 写 + 内存索引增量更新 |
| ⑤ 极致资源效率 | ✅ | 10k 规则 < 50MB 内存 + 30MB 磁盘 |

### 核心路径 vs 侧模块的依赖隔离

```
核心查询路径（必须零外部依赖）:
  /search → EnhancedEverythingIndex(内存) → 返回
  依赖: Python 标准库 only ✅

管理/增强路径（可选，可降级）:
  dep_miner      → scipy.sparse (可选)  → 无则用纯 Python 降级
  gap_detector   → sklearn (可选)       → 无则用纯 Python 降级
  ai_bridge      → requests (HTTP)      → 默认关闭
  SQLite         → sqlite3 (标准库)     → 不可用则只读模式
```

**Everything 哲学的核心是"核心路径零外部依赖"，而不是"整个系统零外部依赖"。** 侧模块引入可选依赖并优雅降级，不违反该原则。

### v3.0 设计原则

1. **Everything 铁律优先** — 核心查询路径零外部依赖，SQLite 故障时仍可查询
2. **组件独立开闭** — 每个模块可独立启用/禁用，互不影响
3. **全量向后兼容** — v1.0/v2.0 API 和 JSONL 数据格式不变
4. **优雅降级** — 任何侧模块故障不影响核心检索
5. **可观测优先** — 每个自动操作都有审计日志，可追溯

---

## 二、全自动架构

```
┌──────────────────────────────────────────────────────────────────┐
│                     v3.0 自动运行架构                             │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  核心查询路径 (Everything 铁律)                                    │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │  HTTP → FastAPI → EnhancedEverythingIndex → 返回         │     │
│  │  零外部依赖，纯内存，SQLite 挂了也能查                      │     │
│  └─────────────────────────────────────────────────────────┘     │
│                                                                  │
│  管理循环 (独立线程，不阻塞查询)                                   │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │  每 30 秒 tick:                                           │     │
│  │  ┌───────────────────────────────────────────────────┐   │     │
│  │  │ ① 收集指标 → ② 模块调度器（逐一执行，故障隔离）      │   │     │
│  │  │    ├─ dep_miner (可选, scipy)                     │   │     │
│  │  │    ├─ auto_proposer (含熔断器)                    │   │     │
│  │  │    ├─ gap_detector (可选, sklearn)                │   │     │
│  │  │    ├─ auto_evolver (含快照)                       │   │     │
│  │  │    └─ ai_ingest (默认关闭)                        │   │     │
│  │  │ ③ 熔断器检查 → ④ 执行 → ⑤ 验证 → ⑥ 失败? → 回滚  │   │     │
│  │  └───────────────────────────────────────────────────┘   │     │
│  └─────────────────────────────────────────────────────────┘     │
│                                                                  │
│  存储层                                                          │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │  SQLite (rules.db)  ← 持久化                              │     │
│  │  内存索引 ← 启动时从 SQLite 加载，运行时增量同步            │     │
│  │  快照 ← 执行前全量备份 SQLite 文件（~30MB）                │     │
│  └─────────────────────────────────────────────────────────┘     │
│                                                                  │
│  告警出口                                                         │
│  ┌─────────────────────────────────────────────────────────┐     │
│  │  审计日志 (audit_log) → 可选 Webhook (Slack/企微)        │     │
│  └─────────────────────────────────────────────────────────┘     │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 三、模块详细设计

### 模块 1：dep_miner — 自动依赖挖掘

**文件**: `dep_miner.py`

**替代**：原因果推理引擎 + 人工 YAML 标注

**思路**：不依赖人工标注，从使用数据和内容中自动挖掘规则间关系。

**三路挖掘**（按优先级）：
```
1. 共现分析 (PMI)
   同一查询结果中频繁同时出现 → 规则间存在关联
   矩阵: scipy.sparse.csr_matrix（可选依赖）
   降级: 纯 Python dict 计算（精度略低，无需 scipy）

2. 时序转移 (Markov)
   规则 A 命中后 → 下一次查询命中 B → A→B 转移关系
   矩阵: 纯 Python defaultdict，无外部依赖

3. 内容相似度
   标题/标签 TF-IDF 余弦相似度 > 0.7 → 潜在重复/冲突
   可选用 sklearn，无则用纯 Python 计数向量化
```

**PMI 冷启动处理**：query_log < 100 条时，仅启用内容相似度挖掘，仪表板显示"依赖图构建中(%)"。

**关系类型区分**（修复"共现≠依赖"逻辑缺陷）：
```python
@dataclass
class Relation:
    source: str
    target: str
    relation_type: RelationType  # RELATED | DEPENDS | CONFLICTS
    strength: float              # 0-1
    source_evidence: str         # "cooccurrence" | "content_sim" | "transfer"

# RELATED: 共现挖掘 → 虚线展示（UI 区分）
# DEPENDS: 共现+转移概率均高 → 实线展示
# CONFLICTS: 内容相似度高但置信度矛盾 → 红色展示
```

**数据持久化**：
```sql
CREATE TABLE rule_relations (
    source_id TEXT NOT NULL,
    target_id TEXT NOT NULL,
    relation_type TEXT NOT NULL,  -- 'related' | 'depends' | 'conflicts'
    strength REAL NOT NULL DEFAULT 0.5,
    evidence TEXT NOT NULL,       -- 'cooccurrence' | 'content_sim' | 'transfer'
    mined_at TEXT NOT NULL,
    PRIMARY KEY (source_id, target_id, relation_type)
);
```

**API**：
```
GET  /deps/graph         → 依赖图（含类型区分）
GET  /deps/chain/{id}    → 影响链
GET  /deps/conflicts     → 冲突规则对
POST /deps/refresh       → 重新挖掘
```

---

### 模块 2：auto_proposer — 全自动提案系统

**文件**: `auto_proposer.py`

**核心机制**：熔断器 + 去重 + 冷却 + 自动快照 + 回滚

**工作流**：
```
检测到异常/机会
  ↓
熔断器检查 ← 连续失败 > 3 次? → 跳过，等待恢复超时
  ↓
去重检查 ← 同类提案已在执行? → 跳过
  ↓
冷却检查 ← 上次执行 < 4 小时前? → 跳过
  ↓
生成提案 + 风险评分 (0-1)
  ↓
执行前快照
  ↓
执行 → 验证（滑动窗口统计检验）→ 成功? → 提交
                                    ↓ 失败
                              自动回滚 + 审计
                                    ↓
                              熔断器计数 +1
```

**提案去重**：
```python
def dedup_key(proposal) -> str:
    return f"{proposal.type}:{proposal.target}"
    # 例: "index_rebuild:main" → 同一类型同一目标不会重复执行
```

**冷却期配置**：
```yaml
proposal_system:
  cooldown_hours:
    index_rebuild: 24       # 索引重建一天最多一次
    cache_tune: 4
    quality_scan: 12
    cold_archive: 168       # 冷数据归档一周一次
    dep_refresh: 24
  circuit_breaker:
    failure_threshold: 3
    recovery_timeout_sec: 300
```

**自动执行的提案类型**：
```yaml
缓存预热:         高频查询 → 预热队列              # 零风险，无快照
低置信度降权:     置信度<0.2 && 负反馈率>30%       # 低风险，无快照
冲突标注:         检测到冲突 → 添加 relation       # 低风险，无快照
索引优化:         平均延迟 > 100ms                 # 中风险，快照+回滚
缓存调优:         命中率 < 40% → 调阈值            # 低风险，无快照
冷数据归档:         365天未命中 → 移入 cold 表       # 低风险，无快照
质量扫描:         每天 03:00                      # 中风险，快照+回滚
依赖重挖掘:       每天 02:00                      # 低风险，无快照
```

**快照机制**：
```python
def create_snapshot() -> str:
    """执行前创建快照 = cp SQLite 文件 + 导出内存索引。"""
    snapshot_id = f"snap_{datetime.now().strftime('%Y%m%d_%H%M%S')}"
    snapshot_path = os.path.join(SNAPSHOT_DIR, f"{snapshot_id}.db")
    # 1. 锁 SQLite 确保一致性
    db.execute("BEGIN IMMEDIATE")
    # 2. 复制文件
    shutil.copy2(DB_PATH, snapshot_path)
    db.execute("COMMIT")
    # 3. 记录
    db.execute("INSERT INTO snapshots VALUES (?, ?, ?, ?, ?)",
               [snapshot_id, datetime.now().isoformat(), len(rules), index.version, snapshot_path])
    return snapshot_id

def rollback_to_snapshot(snapshot_id: str):
    """回滚 = 停止服务 → 替换 SQLite 文件 → 重建索引 → 重启。"""
    # 安全：确保当前操作已停止
    snapshot_path = get_snapshot_path(snapshot_id)
    # 1. 备份当前状态（回滚前的状态，用于事后分析）
    crash_snapshot = create_snapshot()  # 回滚前先保存崩溃现场
    # 2. 覆盖 SQLite
    shutil.copy2(snapshot_path, DB_PATH)
    # 3. 重建内存索引
    rebuild_index_from_sqlite()
```

---

### 模块 3：gap_detector — 知识缺口检测

**文件**: `gap_detector.py`

**功能**：自动识别"用户常问但系统没规则覆盖"的领域。

**流程**（含动态阈值 + 数量上限）：
```
查询日志 (按周)
  ↓
TF-IDF 向量化 (max_features=5000，可选 sklearn)
  ↓
与规则标题/标签余弦相似度
  ↓
相似度 < 动态阈值 → 标记为缺口候选
  ↓
按 frequency × novelty 排序 → 取 top MAX_NEW_RULES_PER_WEEK
  ↓
聚合相似缺口 (cosine > 0.8) → 合并
  ↓
输出 top-N → 自动提交 AI 生成候选项
```

**动态阈值**：
```python
# 上周通过率决定本周严格度
if last_week_pass_rate < 0.5:
    threshold *= 1.1  # 更严格
elif last_week_pass_rate > 0.8:
    threshold *= 0.9  # 更宽松
threshold = clamp(threshold, 0.15, 0.5)

# 每周最多提炼 15 条
MAX_NEW_RULES_PER_WEEK = 15
```

**API**：
```
GET  /coverage              → 热力图数据
GET  /coverage/gaps         → 知识缺口（按 severity 排序）
GET  /coverage/trend        → 覆盖度趋势
```

---

### 模块 4：auto_evolver — 自动演化引擎

**文件**: `auto_evolver.py`

**核心**：基于策略模板 + 安全快照 + 统计验证

**策略模板**（自动匹配当前状态）：
```python
STRATEGIES = {
    "index_optimize": {
        "trigger": lambda m: m["avg_latency_ms"] > 100,
        "risk": "medium",
        "snapshot": True,
        "actions": [AnalyzePatterns(), RebuildIndex()],
        "validation": [LatencyCheck(samples=15, max_increase=0.5)],
    },
    "cache_tune": {
        "trigger": lambda m: m["cache_hit_rate"] < 0.4,
        "risk": "low",
        "snapshot": False,
        "actions": [AnalyzeAccessPatterns(), AdjustThreshold(direction="auto")],
        "validation": [HitRateCheck(samples=30, min_improvement=0.05)],
    },
    "quality_scan": {
        "trigger": lambda m: is_scheduled("03:00"),
        "risk": "medium",
        "snapshot": True,
        "actions": [HealthScan(), AntibodyGenerate(), WeakRuleFix()],
        "validation": [ScoreCheck(samples=50, min_increase=0.02)],
    },
    "cold_archive": {
        "trigger": lambda m: is_scheduled("weekly:04:00"),
        "risk": "low",
        "snapshot": False,
        "actions": [FindColdRules(days=365), ArchiveToCold()],
        "validation": [SizeCheck(max_archived=500)],
    },
    "dep_refresh": {
        "trigger": lambda m: is_scheduled("02:00"),
        "risk": "low",
        "snapshot": False,
        "actions": [RemineCooccurrence(), UpdateDepGraph()],
        "validation": [],
    },
}
```

**统计验证**（修复"单点误判"缺陷）：
```python
def validate_result(before_metrics: list, after_metrics: list,
                    min_samples=10) -> ValidationResult:
    """
    滑动窗口统计检验。
    Cohen's d > 0.5 且方向为退化 → 回滚。
    样本不足 → 暂不判定。
    """
    if len(after_metrics) < min_samples:
        return ValidationResult(passed=True, reason="样本不足，跳过验证")

    mu_before = np.mean(before_metrics)
    mu_after = np.mean(after_metrics)
    sigma = max(np.std(before_metrics), 0.01)
    effect_size = (mu_after - mu_before) / sigma

    if effect_size > 0.5:  # 中效应量，显著退化
        return ValidationResult(
            passed=False,
            reason=f"显著退化: effect_size={effect_size:.2f}, "
                   f"{mu_before:.1f} → {mu_after:.1f}"
        )
    return ValidationResult(passed=True, reason=f"正常: effect_size={effect_size:.2f}")
```

**快照分级保留**：
```python
# max_snapshots=50, 但新快照写入时淘汰最旧的
def prune_snapshots(max_keep=50):
    snapshots = db.execute(
        "SELECT id, timestamp FROM snapshots ORDER BY timestamp"
    ).fetchall()
    while len(snapshots) > max_keep:
        oldest = snapshots.pop(0)
        os.remove(get_snapshot_path(oldest["id"]))
        db.execute("DELETE FROM snapshots WHERE id = ?", [oldest["id"]])
```

---

### 模块 5：ai_bridge + auto_ingest — AI 自动学习

**文件**: `ai_bridge.py`、`auto_ingest.py`

**AI 验证修复**——区分三种评分结果：

```python
class AIValidationResult(Enum):
    CONSISTENT   = "consistent"    # 与系统知识一致 → 可信
    PARTIAL      = "partial"       # 部分一致 → 部分可信
    UNVERIFIABLE = "unverifiable"  # 系统无相关知识 → 无法验证
    CONTRADICT   = "contradict"    # 与系统知识矛盾 → 不可信
```

**工作流**：
```
用户查询
  ↓
系统搜索 → 置信度 ≥ 0.7? → 直接返回
  ↓ 置信度 < 0.7 (且 AI Bridge 未超预算)
AI 生成回答
  ↓
系统规则验证 AI 答案:
  → CONTRADICT（与系统知识矛盾） → 返回系统结果 + 标注矛盾
  → UNVERIFIABLE（系统无相关知识） → 返回 AI 回答 + 标注"未经系统验证"
  → CONSISTENT/PARTIAL → 返回 AI 回答 + 系统验证标签
  ↓ (异步)
auto_ingest 学习:
  1. 提取可复用知识点
  2. 生成规则草稿
  3. 去重检测（标题 TF-IDF + 内容哈希双路去重）
  4. 不重复 → 自动入库，verifier=ai, confidence=0.5
  5. 重复 → 丢弃
  ↓
后续用户反馈自动调整置信度
```

**AI 预算控制**：
```python
class AIBudget:
    def __init__(self, daily_limit_usd=5.0):
        self.daily_limit = daily_limit_usd
        self.cost_today = 0.0
        self.last_reset = date.today()

    def allow_call(self) -> bool:
        if date.today() > self.last_reset:
            self.cost_today = 0.0
            self.last_reset = date.today()
        return self.cost_today < self.daily_limit

    def record_call(self, cost_usd):
        self.cost_today += cost_usd
        if self.cost_today >= self.daily_limit:
            logger.warning("AI_BUDGET_EXCEEDED",
                           f"今日 AI 费用 ${self.cost_today:.2f} 已达上限")
```

**API**：
```
POST /ai/query     → AI 增强查询（需启用 + 预算充足）
GET  /ai/budget    → 当前预算状态
GET  /ai/stats     → AI 学习统计
```

---

## 四、修复项对应关系

| # | 问题 | 严重度 | 修复方式 |
|---|------|--------|---------|
| 1 | 查询日志无限增长 | 🟡 | 8 周轮换策略 + `DELETE FROM query_log WHERE timestamp < date('now', '-56 days')` |
| 2 | 内存索引与 SQLite 不同步 | 🟡 | 双写 + 每日 `reconcile()` 对账 |
| 3 | PMI 冷启动空结果 | 🟡 | query_log<100 时改用内容相似度降级 |
| 4 | 无 AI 预算控制 | 🟡 | `AIBudget` 每日限额 |
| 5 | 冷规则死循环 | 🟡 | 归档入 `rules_cold` 表，缺口检测时解冻 |
| 6 | 管理面板未更新 | 🟡 | 加 `/v3/status` 汇总接口 |
| 7 | 无告警出口 | 🟡 | `AlertChannel` 抽象（LogAlert + 可选 Webhook） |
| 8 | AI 验证矛盾 | 🔴 | 区分 UNVERIFIABLE/PARTIAL/CONTRADICT |
| 9 | 共现≠依赖 | 🔴 | RELATED/DEPENDS/CONFLICTS 三层区分 |
| 10 | 单点性能误判 | 🔴 | 滑动窗口统计检验 (Cohen's d) |
| 11 | 缺口阈值风暴 | 🟡 | 动态阈值 + 每周上限 15 条 |
| 12 | 提案重复触发 | 🟡 | `dedup_key` + `cooldown` 冷却期 |
| 13 | 快照数量失控 | 🟡 | 新快照淘汰最旧策略 |
| 14 | 熔断器 | 🔵 | `CircuitBreaker` 每类提案独立熔断 |
| 15 | 模块故障隔离 | 🔵 | 主循环每个模块独立 try/except |
| 16 | 优雅降级 | 🔵 | SQLite 不可用→只读，AI 不可用→v2.0 回退 |
| 17 | 操作超时 | 🔵 | `run_with_timeout()` 各模块独立超时 |
| 18 | 启动自检 | 🔵 | 表完整性、索引一致性、配置有效性 |
| 19 | 审计日志 | 🔵 | `audit_log` 表，不参与系统回滚 |
| 20 | 运行时热配置 | 🔵 | `config_runtime` 表，`POST /v3/config` |
| 21 | 主循环心跳 | 🔵 | 独立心跳线程 + `/v3/health` |
| 22 | 级联回滚 | 🔵 | 回滚前版本检查 + 冲突检测 |
| 23 | 数据完整性校验 | 🔵 | 每日 integrity check |

> 🔴 = 逻辑缺陷，🟡 = 功能遗漏，🔵 = 健壮性

---

## 五、存储层 Schema（含审查修复）

```sql
-- rules.db

-- 主规则表
CREATE TABLE rules (
    id TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    content TEXT NOT NULL,
    category TEXT NOT NULL DEFAULT 'general',
    tags TEXT NOT NULL DEFAULT '[]',
    confidence REAL NOT NULL DEFAULT 0.5,
    verifier TEXT NOT NULL DEFAULT 'manual',
    version INTEGER NOT NULL DEFAULT 1,
    parent_id TEXT,
    duplicate_of TEXT,
    evolution_log TEXT NOT NULL DEFAULT '[]',
    hit_count INTEGER NOT NULL DEFAULT 0,
    created_at TEXT NOT NULL,
    last_hit TEXT,
    expires_at TEXT,
    ai_verified INTEGER NOT NULL DEFAULT 0,
    last_ai_review TEXT,
    -- 新增：上次版本快照时的版本号（用于回滚冲突检测）
    snapshot_version INTEGER DEFAULT 0
);

-- 冷规则表（修复冷规则死循环）
CREATE TABLE rules_cold (
    LIKE rules,
    archived_at TEXT NOT NULL,
    archive_reason TEXT NOT NULL DEFAULT 'inactive_365d'
);

-- 规则关系表（区分类型，修复共现≠依赖）
CREATE TABLE rule_relations (
    source_id TEXT NOT NULL,
    target_id TEXT NOT NULL,
    relation_type TEXT NOT NULL,  -- 'related' | 'depends' | 'conflicts'
    strength REAL NOT NULL DEFAULT 0.5,
    evidence TEXT NOT NULL,       -- 'cooccurrence' | 'content_sim' | 'transfer'
    mined_at TEXT NOT NULL,
    PRIMARY KEY (source_id, target_id, relation_type)
);

-- 查询日志（8 周轮换）
CREATE TABLE query_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp TEXT NOT NULL,
    query_text TEXT NOT NULL,
    latency_ms REAL,
    result_count INTEGER,
    cache_hit INTEGER
);

-- 指标日志
CREATE TABLE metrics_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp TEXT NOT NULL,
    metric_name TEXT NOT NULL,
    metric_value REAL NOT NULL
);

-- 提案记录
CREATE TABLE proposals (
    id TEXT PRIMARY KEY,
    title TEXT NOT NULL,
    description TEXT NOT NULL,
    module TEXT NOT NULL,
    dedup_key TEXT NOT NULL,       -- 用于去重
    priority INTEGER NOT NULL DEFAULT 3,
    risk_score REAL DEFAULT 0.0,
    status TEXT NOT NULL DEFAULT 'pending',
    snapshot_id TEXT,
    result TEXT,
    error_message TEXT,
    created_at TEXT NOT NULL,
    executed_at TEXT
);

-- 系统快照
CREATE TABLE snapshots (
    id TEXT PRIMARY KEY,
    timestamp TEXT NOT NULL,
    rules_count INTEGER NOT NULL,
    index_version INTEGER NOT NULL,
    storage_path TEXT NOT NULL
);

-- 审计日志（不可变，不参与回滚）
CREATE TABLE audit_log (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    timestamp TEXT NOT NULL,
    action TEXT NOT NULL,
    module TEXT NOT NULL,
    actor TEXT NOT NULL,
    target TEXT,
    before_snapshot TEXT,
    after_snapshot TEXT,
    result TEXT NOT NULL,
    duration_ms INTEGER,
    error_message TEXT
);

-- 运行时可热加载配置
CREATE TABLE config_runtime (
    key TEXT PRIMARY KEY,
    value TEXT NOT NULL,
    updated_at TEXT NOT NULL
);

-- 索引
CREATE INDEX idx_rules_category ON rules(category);
CREATE INDEX idx_rules_confidence ON rules(confidence);
CREATE INDEX idx_rules_hit_count ON rules(hit_count);
CREATE INDEX idx_query_ts ON query_log(timestamp);
CREATE INDEX idx_metrics_ts ON metrics_log(timestamp);
CREATE INDEX idx_proposals_status ON proposals(status);
CREATE INDEX idx_audit_module ON audit_log(module);
CREATE INDEX idx_audit_result ON audit_log(result);
CREATE INDEX idx_relations_type ON rule_relations(relation_type);
```

---

## 六、Everything 哲学合规性验证

### 验证 1：核心路径零外部依赖

```
查询链路: HTTP → FastAPI → EnhancedEverythingIndex(内存) → JSON 返回
依赖:      Python 标准库 only
SQLite:    不在核心路径上，故障时降级为最终成功加载的内存索引
```

**SQLite 故障测试**：
```python
# 伪代码：SQLite 不可用时系统的行为
try:
    db.connect()
except DatabaseError:
    # 降级为只读模式
    system_mode = INDEX_ONLY
    # 查询路径仍然可用（从内存索引读取）
    # 写入操作（AI 入库、提案执行等）全部跳过
    # 定时重试 SQLite
```

### 验证 2：组件独立开闭

```yaml
v3:
  dep_miner:
    enabled: true          # 关闭后依赖图不更新，但不影响查询
  proposal_system:
    enabled: true          # 关闭后无自动操作，系统退化为 v2.0
  gap_detector:
    enabled: true          # 关闭后无知识缺口检测
  auto_evolver:
    enabled: true          # 关闭后无自动演化
  ai_bridge:
    enabled: false         # 默认关闭，不影响其他任何功能
```

### 验证 3：全量向后兼容

```
v1.0/v2.0 API → 全部不变，新增 v3.0 路由独立前缀
JSONL 数据 → migrate_v3.py 导入 SQLite，JSONL 保留备份
RuleStorage 接口 → storage_v2.py 实现相同接口，调用方零修改
```

### 验证 4：优雅降级

```python
DEGRADATION_TABLE = {
    "sqlite_down": {
        "effect": "只读模式，写入/演化/AI 入库跳过",
        "query_path": "正常",
    },
    "ai_api_down": {
        "effect": "低置信度时仍返回系统结果，不调 AI",
        "query_path": "正常",
    },
    "sklearn_missing": {
        "effect": "gap_detector 降级为纯 Python 计数向量化",
        "query_path": "不受影响",
    },
    "scipy_missing": {
        "effect": "dep_miner 降级为纯 Python dict 计算",
        "query_path": "不受影响",
    },
    "index_corrupted": {
        "effect": "自动从 SQLite 重建",
        "query_path": "重建期间不可用（< 1秒）",
    },
}
```

### 验证 5：可观测性

```
每个自动操作 → 写入 audit_log（不可变）
主循环 → 心跳更新，/v3/health 可查
每个模块 → 独立健康状态
告警 → 日志默认，Webhook 可选
```

---

## 七、实施计划（3 Phase，6 周）

### Phase A：存储升级 + 核心查询 + 依赖挖掘（第 1-2 周）

| 任务 | 文件 | 预估 |
|------|------|------|
| A1. SQLite 存储层（兼容现有接口） | `storage_v2.py` | 2天 |
| A2. 内存索引双写 + 对账 | `storage_v2.py` + `index.py` | 1天 |
| A3. 数据迁移脚本 (JSONL → SQLite) | `migrate_v3.py` | 1天 |
| A4. 自动依赖挖掘 + 关系类型区分 | `dep_miner.py` | 2天 |
| A5. 依赖图 API | `main.py` | 0.5天 |
| A6. 冷规则表 + 归档/解冻 | `storage_v2.py` | 0.5天 |
| A7. 测试 + 冷启动处理 | `tests/` | 1天 |

### Phase B：全自动提案 + 知识缺口 + 安全机制（第 3-4 周）

| 任务 | 文件 | 预估 |
|------|------|------|
| B1. 自动提案系统（熔断器+去重+冷却） | `auto_proposer.py` | 2天 |
| B2. 快照机制 + 回滚 + 级联冲突检测 | `snapshot.py` | 1.5天 |
| B3. 提案自动执行 + 统计验证 | `auto_proposer.py` | 1天 |
| B4. 知识缺口检测（sklearn 可选降级） | `gap_detector.py` | 2天 |
| B5. 动态阈值 + 数量上限 | `gap_detector.py` | 0.5天 |
| B6. 覆盖 API | `main.py` | 0.5天 |
| B7. 查询日志轮换 + 指标清理 | 内置 | 0.5天 |
| B8. 集成测试 | `tests/` | 1天 |

### Phase C：AI 学习 + 自动演化 + 运维设施（第 5-6 周）

| 任务 | 文件 | 预估 |
|------|------|------|
| C1. AI 桥接层（含预算控制+验证修复） | `ai_bridge.py` | 2天 |
| C2. AI 自动提炼 + 双路去重 | `auto_ingest.py` | 1.5天 |
| C3. 自动演化引擎 + 策略模板 | `auto_evolver.py` | 2天 |
| C4. 主循环 + 模块调度 + 心跳 + 故障隔离 | `main.py` 扩展 | 1.5天 |
| C5. 审计日志 + 告警出口 + 热配置 | `audit.py` + `alert.py` | 1天 |
| C6. 启动自检 + 数据完整性校验 | `health.py` | 1天 |
| C7. 全量测试 + 降级测试 | `tests/` | 1天 |
| C8. 管理面板 `/v3/status` | `static/` | 0.5天 |

---

## 八、Everything 哲学 vs v3.0 新增复杂度

```
v2.0:   1 个索引文件 + 1 个存储文件 + 1 个 API 入口 = 简单、直接
                             ↓
v3.0:   5 个核心模块 + 快照 + 审计 + 主循环 + 告警 + 热配置
                             ↓
这确实是复杂度增加，但代价是：

v2.0 不能做的:
  - 自动发现用户知识缺口
  - 自动优化自身性能
  - 自动清理过期规则
  - 自动从 AI 学习新知识
  - 知道改 A 会影响 B

v3.0 不牺牲的:
  ✅ 查询路径仍是纯内存索引，零外部依赖
  ✅ 任何一个新模块故障都不会影响"查规则"这个核心能力
  ✅ v1.0/v2.0 API 和数据结构完全不变
  ✅ 新模块全部可选，可独立开关

一句话: 复杂度增加在"侧链"，铁律守护在"核心路径"。
```
